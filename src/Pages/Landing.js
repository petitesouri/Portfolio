import useFetch from "../Components/useFetch";

const Landing = () => {
    console.log(useFetch())
    return (
        <main>
            <div>BONJOUR</div>
        </main>
    )
}

export default Landing;